# URL base do backend (REST + Socket.io fallback)
# Dev:  http://localhost:3001
# Prod: https://seuapp.onrender.com
VITE_API_URL=http://localhost:3001

# URL do Socket.io (opcional — se não setar usa VITE_API_URL)
# Útil quando o WebSocket roda em domínio separado do REST
VITE_SOCKET_URL=
